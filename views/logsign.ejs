<%- include('partials/header') -%>

<!-- LEON'S ORIGINAL AUTH -->

<!-- <main class="container">
  <div class="row justify-content-center">
    <section class="col-6 mt-5">
      <% if (locals.messages.errors) { %> <% messages.errors.forEach( el => { %>
      <div class="alert alert-danger"><%= el.msg %></div>
      <% }) %> <% } %> <% if (locals.messages.info) { %> <%
      messages.info.forEach( el => { %>
      <div class="alert alert-info"><%= el.msg %></div>
      <% }) %> <% } %>
      <form action="/login" method="POST">
        <div class="mb-3">
          <label for="exampleInputEmail1" class="form-label"
            >Email address</label
          >
          <input
            type="email"
            class="form-control"
            id="exampleInputEmail1"
            aria-describedby="emailHelp"
            name="email"
          />
        </div>
        <div class="mb-3">
          <label for="exampleInputPassword1" class="form-label">Password</label>
          <input
            type="password"
            class="form-control"
            id="exampleInputPassword1"
            name="password"
          />
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
      </form>
    </section>
  </div>
</main> -->

<!-- LEON'S ORIGINAL AUTH -->

<!-- GOOGLE OAUTH2.0 -->
<main class="container d-flex justify-content-center pt-5">
  <div class="card text-center" style="width: 18rem;">
    <img src="/imgs/annie-spratt-htGAp5LIBhI-unsplash.jpg" alt="Forager's haul of mushrooms with identification" class="card-img-top login-img">
    <div class="card-body">
      <h5 class="card-title">Login/Signup!</h5>
      <p class="card-text">Create a record of your forages!</p>
      <a href="/login" class="btn btn-primary btn-block">Continue with Email</a>
      <a href="#" class="btn btn-primary btn-block">Continue with Google</a>
    </div>
  </div>
</main>

<%- include('partials/footer') -%>
